
const initialState = {
    orders: [],
};

export default function orderListReducer(state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
}
